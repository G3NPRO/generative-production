# Setup and Safety

The things that will hurt you if you skip them. Credentials, billing, and what not to push to git.

## Planned topics

- **Managing credentials** — `.env` files, never committing secrets, password-manager patterns. ([page](managing-credentials.md))
- **API keys** — what they are, how billing works, rate limits, spending caps.
- **Repos and `.gitignore`** — what *never* goes into a public repo.
- **Sandboxing** — when to trust an agent to run commands.
